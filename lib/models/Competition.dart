class Competition {
  String id;
  String title;
  String imageUrl;
  String description;
  String createdBy;
  String createdDate;
  String updatedDate;

  Competition(this.id, this.title, this.imageUrl, this.description,
      this.createdBy, this.createdDate, this.updatedDate);
}