class UserStore {
  String uid;
  String name;
  String phone;
  String email;
  String password;
  String createdAt;
  String updatedAt;

  UserStore(this.uid, this.name, this.phone, this.email, this.password,
      this.createdAt, this.updatedAt);
}